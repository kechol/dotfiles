### backlog/
- セッション開始時に `INDEX.md` を読む。個別ファイルは関係するものだけ開く
- 新規タスク: `task-<3桁連番>-<kebab-case>.md` を作り、INDEX に1行追加する
- 着手時: `status: doing` にし、受け入れ条件とスコープ外を書く。
  実装計画はまず私に提示して**承認を待ってからコードを書く**
- 完了時: 完了サマリを3行以内で書き、`archive/` へ移動し、INDEX の完了欄へ移す
- 状態を変えたら **同じ commit で INDEX も更新する**
